# {{ISSUE_TITLE}}

> Garnish Linear issue (team Loom, project per `.agents/envelope/linear-map.md`).
> One tracer-bullet slice that cuts through every layer and is demoable on its own.

## Context

Why this slice exists and the spec section it implements, in glossary terms
(harness, TUI, quest, pack, verifier, progression, gate, tutor).

## Acceptance criteria

- [ ] {{observable outcome}}
- [ ] {{observable outcome}}

## Scope / non-goals

- In scope: {{the thin vertical slice}}
- Non-goals: {{what this issue does not touch}}

## Dependencies

- Blocked by: {{LOO ids}}
- Blocks: {{LOO ids}}

## Execution

- Mode: {{HITL | AFK}} (label from the Mode group)
- Branch: carries this issue's `LOO-NNN` id so the PR auto-links and the merge
  auto-closes the issue.

## Proof

How to prove this slice works on its own without expanding scope
(`bun run typecheck`, `bun test`, targeted e2e, or live walkthrough evidence).
